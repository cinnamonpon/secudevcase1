Your cart: <br>
<table style="width:50%;">
  <tr>
    <th>Item</th>
    <th>Quantity</th>
    <th>Price</th>
    <th>Total</th>
  </tr>
  <% @cart_items.each do |c| %>
    <tr>
      <td><%= c.item.name %></td>
      <td><%= c.quantity %></td>
      <td>$<%= c.price %></td>
      <td>$<%= c.quantity * c.price %>
    </tr>
  <% end %>
</table>
<%= link_to "Check out" %>
